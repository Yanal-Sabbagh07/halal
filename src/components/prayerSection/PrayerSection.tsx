import React from "react";

const PrayerSection = () => {
  return (
    <section id="prayer" className="h-screen w-full bg-red-500 ">
      <h1></h1>
    </section>
  );
};

export default PrayerSection;
